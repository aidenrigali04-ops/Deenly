/**
 * Re-export shared rewards config for Metro — single import path for the app (`@/lib/rewards`).
 * Source of truth: `../../../../shared/rewards` at repo root.
 */
export * from "../../../../shared/rewards";
